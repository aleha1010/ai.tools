---
id: T004
dependencies: [T003]
---
# T004: Dependency Dependent (Blocked Until T003)

## Context
Verify dependencies checking logic.
This task should be BLOCKED until T003 is complete.

## Test Specification

### First Attempt
1. Ralph Loop checks T004 dependencies
2. T003 status: incomplete (space in [ ])
3. T004 BLOCKED → skipped
4. Ralph Loop processes T003 first

### After T003 Complete
1. T003 marked [x]
2. Status cache rebuilt
3. T004 dependencies check: T003=x → PASS
4. T004 becomes executable
5. Execute, review, approve

## Implementation Instructions

Create file that extends T003 config:
```bash
cat >> app_config.yaml << 'YAML'

database:
  host: localhost
  port: 5432
YAML
```

Create pending file:
```bash
cat > .ralph_pending_tasks.json << 'JSON'
{"task_id": "T004", "files_changed": ["app_config.yaml"], "summary": "Extended config with database settings"}
JSON
```

## Acceptance Criteria
- [ ] T004 not selected while T003 incomplete
- [ ] check_dependencies() returns false initially
- [ ] After T003 done, T004 selected
- [ ] Dependencies enforced correctly
